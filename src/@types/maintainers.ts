export type MaintainerInfo = {
  username: string;
  name: string;
  bio: string | null;
  blog: string | null;
  location: string | null;
};

export type ResumedMaintaners = {
  username: string;
  name: string;
  bio: string;
};
